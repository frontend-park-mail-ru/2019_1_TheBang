mixin button(title, type)
    case type
        when 'primary'
            button.button.button-primary= title

        when 'secondary'
            button.button.button-secondary= title

        when 'vk'
            button.button-vk