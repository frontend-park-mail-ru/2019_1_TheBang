{
  "version": 2,
  "builds": [
    { "src": "package.json", "use": "@now/now-build", "config": { "distDir": "dist" } }
  ],
  "routes": [
    { "src": "/(.+)", "dest": "./$1" },
    { "src": "/", "dest": "/public/" }
  ]
}